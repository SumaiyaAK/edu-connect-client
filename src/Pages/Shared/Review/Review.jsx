

const Review = ({ review }) => {
    const { College_Name, Image, Admission_Dates, Admission_Process, Events, Research_History, Sports } = review;
    return (
        <div>
            
        </div>
    );
};

export default Review;